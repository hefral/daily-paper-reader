---
title: Teacher-Student Representational Alignment for Reinforcement Learning-Driven Imitation Learning
title_zh: 面向强化学习驱动的模仿学习的师生表征对齐
authors: "Meraj Mammadov, Pedro Zuidberg Dos Martires, Johannes Andreas Stork"
date: 2026-05-27
pdf: "https://arxiv.org/pdf/2605.28372v1"
tags: ["query:rl-humanoid"]
score: 9.0
evidence: 提出了基于强化学习的机器人模仿学习中表征对齐方法
tldr: 在强化学习驱动的模仿学习中，教师策略依赖特权状态信息导致学生模仿存在不可消除的差距。本文提出一种新的表征对齐方法，通过自监督对比学习训练共享嵌入空间，隐藏智能体特定观测，从而构建可模仿的教师策略。在多领域评估中，学生性能显著提升，模仿差距大幅缩小，为高维观测下的高效模仿学习提供了新思路。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.28372v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 842, \"height\": 838, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28372v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 645, \"height\": 528, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28372v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 875, \"height\": 296, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28372v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 654, \"height\": 334, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.28372v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1814, \"height\": 555, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.28372v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 872, \"height\": 253, \"label\": \"Table\"}]"
motivation: 解决教师策略依赖特权信息而学生无法从观测中推断时产生的不可消除模仿差距。
method: 通过自监督对比学习并行训练共享嵌入空间，限制梯度更新以隐藏私有信息，使教师策略可模仿。
result: 在多个示例域中，学生性能优于现有方法，模仿差距显著减小。
conclusion: 本算法在不需额外强化学习微调的情况下，有效缩小了模仿差距，提升了学生策略性能。
---

## 摘要
模仿学习（IL）从基于状态的强化学习（RL）策略是一种常见方法，用于克服机器人领域中复杂且高维观察空间普遍存在的维度灾难。本文解决了当教师策略和学生策略分离学习时出现的不可消除的模仿差距，其中教师策略可以自由依赖特权状态信息，而学生无法从其观察中推断出这些信息。我们并非在模仿学习后用RL微调来改善不佳的学生表现（这通常需要一套全新的训练设置），而是提出了一种新颖算法，该算法学习一个共享嵌入空间，隐藏特定于智能体的观察，从而通过构造来训练可模仿的教师策略。我们以自监督对比学习方式并行于教师策略训练该共享嵌入空间，并通过限制其梯度更新编码器网络来防止其提取私有信息。我们在多个示例领域上进行了评估，并与最先进的基线方法比较，结果表明我们的算法能够显著减少模仿差距，实现更高的学生性能。

## Abstract
Imitation learning (IL) from a state-based reinforcement learning (RL) policy is a common approach to overcome the curse of dimensionality in complex and high-dimensional observation spaces prevalent in robotics. This paper addresses the irreducible imitation gap that emerges when teacher and student are learned in isolation, and the teacher policy has the liberty to rely on privileged state information that the student cannot infer from its observations. Instead of improving poor student performance with RL finetuning after IL, which often requires a whole new training setup, we propose a novel algorithm which learns a shared embedding space that hides agent-specific observations and thus trains imitable teacher policies by construction. We train the shared embedding space with self-supervised contrastive learning in parallel to the teacher policy and prevent it from extracting private information by limiting its gradients from updating the encoder networks. We perform evaluations on several example domains and compare to state-of-the-art baselines showing that our algorithm enables higher student performance with substantially reduced imitation gap.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义

*   **研究动机**：在机器人等复杂领域中，直接用强化学习（RL）在高维观测空间（如图像）训练策略样本效率低；一种常见的解决方案是“先训练低成本状态下的教师策略，再用模仿学习（IL）将知识蒸馏到高维观测的学生策略”。然而，教师策略通常能接触到学生无法观测的**特权状态信息**（例如全局坐标、障碍物完整地图等），这使得教师学会了一些学生无法模仿的行为，从而产生**不可消除的模仿差距**。
*   **整体含义**：现有方法往往通过事后对学生进行RL微调来弥补差距，但这又回到了RL训练困难的起点；或者通过修改教师奖励函数来强制可模仿性，但这可能破坏任务学习或诱发奖励黑客行为。本文提出一种**任务无关**的方法，通过在教师训练阶段就构造一个仅保留师生共享信息的表示空间，**从构造上消除模仿差距**，使得蒸馏出的学生策略无需额外微调即可达到高性能。

## 2. 论文提出的方法论

*   **核心思想**：将师生观测视为同一环境状态的两个不同视图，每个观测由**共享组件（ct）** 和**私密组件（ptT, ptS）** 组成。目标是学习一个共享的嵌入空间，该空间只编码共享信息ct，教师策略在该空间上训练，从而被迫仅依赖学生也能获取的信息做出决策。
*   **关键技术细节**：
    *   **对比学习构建共享空间**：使用**InfoNCE损失**（公式6），以同一时刻的师生观测对作为正样本对，以批次内其他观测对作为负样本，训练两个独立的编码器（分别处理教师、学生观测）将不同模态映射到同一隐空间。该损失理论上在给定假设下可恢复到共享信息ct。
    *   **对齐损失**：额外增加一个**显式对齐损失**（公式7），直接最大化配对嵌入的相似度，进一步促进分布一致。
    *   **稳定性损失**：由于策略网络在嵌入空间上训练且梯度不回传给编码器（防止提取私密信息），为保证编码器持续提供稳定的嵌入，引入**稳定性损失**（公式8），鼓励编码器更新前后策略网络输出的logits保持一致。
    *   **交替训练**：分两阶段迭代——**阶段一**：利用教师滚动收集的配对轨迹数据，通过梯度下降优化编码器损失 Lembedding = Lcontrastive + Lalignment + Lstability；**阶段二**：固定编码器，在学到的嵌入上使用PPO训练教师策略最大化任务回报。策略梯度不会更新编码器。

## 3. 实验设计

*   **实验场景**：
    *   **TunnelVision（网格世界）**：离散网格导航，教师可观察周围8个单元格，学生只能观察前方3个单元格。教师最优策略是直接走向目标，但学生需要频繁转身以避免撞上未观测到的障碍物。
    *   **CollectHealth（3D房间）**：智能体需要收集5个随机生成的生命值箱子，可离散移动和旋转视角（10个动作）。教师能观测箱子的精确坐标，学生通过有限FOV的第一人称图像感知。教师可以不动视角直接去拿所有箱子，但学生必须主动转动视角寻找箱子。
*   **基准方法**：
    *   **BC（行为克隆）**：教师独立训练后，用监督学习方式训练学生模仿教师动作。
    *   **SITT [8]**：联合训练师生，通过在奖励和策略梯度上加入KL散度惩罚项，显式鼓励教师停留在学生可模仿的状态。
*   **评估指标**：学生策略的成功率，以及师生间的模仿差距（教师成功率 - 学生成功率）。

## 4. 资源与算力

*   论文**未明确提及**所使用的GPU型号、数量或总体训练时长。仅提及“每个实验至少重复5个随机种子”，其余硬件和算力细节缺失。

## 5. 实验数量与充分性

*   **实验组数**：在两个环境上进行了实验，每组包括：
    *   本文提出的完整方法（Ours）。
    *   两个消融版本（Ours w/o alignment, Ours w/o stability）。
    *   两个基线（BC, SITT）。
    *   总计约 (2环境 × 5变体) = 10组主要实验，每组5次重复。
*   **充分性与客观性**：
    *   实验覆盖了两种性质不同的私密信息场景（全局位置/地图 vs. 有限FOV），展示了方法的泛化性。
    *   消融实验清晰验证了对齐损失和稳定性损失各自的贡献。
    *   对比的基线SITT是目前最相关的工作之一，比较公平。
    *   但实验**局限于离散动作空间**和相对简单的环境，未在更复杂的连续控制任务上验证，且消融实验仅考察了两个损失项，总数偏少。

## 6. 论文的主要结论与发现

*   所提出的**表征对齐方法**能够在几乎不牺牲教师自身性能的前提下，大幅提升学生策略的性能，将模仿差距降低至接近0（TunnelVision从0.81降至0.00，CollectHealth从0.53降至0.01）。
*   与BC和基于奖励惩罚的SITT相比，本方法在**学生成功率上均取得最优效果**，且无需调优像SITT中α那样的任务-模仿权衡系数。
*   消融实验表明，**对齐损失和稳定性损失**对最终性能均有关键作用，尤其在TunnelVision环境中，移除稳定性损失导致教师和学生成功率均大幅下降。

## 7. 优点

*   **任务无关性**：无需手动的特征工程或对环境动态的先验知识，也**不修改传统RL奖励结构**，避免了任务学习与可模仿性之间的权衡。
*   **即插即用**：可方便地集成到已有的“RL教师+IL学生”框架中，仅需额外收集配对观测和训练编码器，超参数调优负担小。
*   **设计巧妙**：通过阻止策略梯度回流到编码器，从信息流上强制隐藏私密信息；对比学习仅使用时间对齐样本为正对，保留了相邻状态的细微差异，有利于生成精细策略。

## 8. 不足与局限

*   **环境复杂度有限**：仅在离散动作空间、相对简单的两个仿真环境上验证，尚未在更复杂的高维连续控制任务（如灵巧手操作、高速飞行）中测试，方法的可扩展性存疑。
*   **假设条件较强**：核心假设“私密组件在给定共享组件下条件独立”（公式5）在实际多模态传感（如相机+激光雷达）中可能不总是成立，若该假设被违背，对比学习恢复共享信息的效果可能下降。
*   **对比基线较少**：仅与BC和SITT对比，缺少与其他表征对齐类方法或更先进的IL方法的比较。
*   **计算开销不明**：未报告训练对比学习编码器带来的额外计算成本和时间开销，无法评估其在大规模任务上的实用效率。
*   **单任务局限**：未讨论当存在多个下游任务时，是否可以将此共享表征直接复用，目前仅在单个任务上验证。

（完）
