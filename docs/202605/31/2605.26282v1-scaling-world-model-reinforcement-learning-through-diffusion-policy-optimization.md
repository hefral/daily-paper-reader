---
title: Scaling World-Model Reinforcement Learning Through Diffusion Policy Optimization
title_zh: 通过扩散策略优化扩展世界模型强化学习
authors: "Xiaoyuan Cheng, Wenxuan Yuan, Zhancun Mu, Yuanzhao Zhang, Yiming Yang, Hai Wang, Zhuo Sun, Che Liu"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.26282v1"
tags: ["query:rl-humanoid"]
score: 9.0
evidence: 提出基于模型的扩散策略优化以扩展世界模型强化学习
tldr: 基于模型的强化学习中，搜索与价值学习的结构性错位导致训练不一致，严重制约世界模型的可扩展性。本文提出MBDPO框架，它通过扩散策略表示统一了搜索与策略优化，将策略改进重新表述为潜在世界模型中搜索轨迹的扩散过程，并从数据中提取隐式能量函数锚定策略，以精细优化扩散评分场，从而克服搜索与价值学习的错位。在离线多任务预训练、在线学习及离线到在线微调等多个场景中，MBDPO均表现优异，且离线预训练性能随模型容量增大而持续提升。该方法突破了世界模型扩展的关键瓶颈，为可扩展策略学习提供了新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.26282v1/fig-001.webp\", \"caption\": \"\", \"page\": 3, \"index\": 1, \"width\": 1100, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-002.webp\", \"caption\": \"\", \"page\": 3, \"index\": 2, \"width\": 1099, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-003.webp\", \"caption\": \"\", \"page\": 3, \"index\": 3, \"width\": 383, \"height\": 346}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-004.webp\", \"caption\": \"\", \"page\": 3, \"index\": 4, \"width\": 1100, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-005.webp\", \"caption\": \"\", \"page\": 3, \"index\": 5, \"width\": 1100, \"height\": 680}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-006.webp\", \"caption\": \"\", \"page\": 6, \"index\": 6, \"width\": 5192, \"height\": 1650}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-007.webp\", \"caption\": \"\", \"page\": 6, \"index\": 7, \"width\": 4050, \"height\": 1399}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-008.webp\", \"caption\": \"\", \"page\": 21, \"index\": 8, \"width\": 1659, \"height\": 948}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-009.webp\", \"caption\": \"\", \"page\": 21, \"index\": 9, \"width\": 1644, \"height\": 957}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-010.webp\", \"caption\": \"\", \"page\": 22, \"index\": 10, \"width\": 1600, \"height\": 540}, {\"url\": \"assets/figures/arxiv/2605.26282v1/fig-011.webp\", \"caption\": \"\", \"page\": 22, \"index\": 11, \"width\": 1991, \"height\": 789}]"
motivation: 现有世界模型强化学习方法存在搜索与价值学习的结构性错位，导致训练不一致和次优策略，成为规模化瓶颈。
method: 提出MBDPO，以扩散策略表示统一搜索与策略优化，将策略优化重铸为潜在世界模型中搜索轨迹的扩散过程，利用隐式能量函数锚定策略。
result: 在离线预训练、在线学习及离线到在线微调等设置下取得性能提升，离线预训练性能随模型容量单调增长。
conclusion: MBDPO突破了世界模型扩展的关键障碍，为可扩展策略学习开辟了新方向。
---

## 摘要
基于模型的强化学习（RL）可以通过世界模型在大规模下得到有效支持。然而，在实践中，扩展此类方法仍然受到根本性限制。一个公认的挑战是模型偏差和误差累积，这降低了长期预测的质量。除了这些问题，我们指出了一个更为关键但尚未被充分探索的瓶颈：现有世界模型方法中搜索与价值学习之间的结构性错位。特别是，策略改进通常依赖于由另一个非搜索策略导出的价值函数，导致训练不一致，最终造成次优学习。为了解决这一局限，我们在世界模型中提出了基于模型的扩散策略优化（MBDPO），该框架通过扩散策略表示统一了搜索与策略优化，从而释放了世界模型在可扩展策略学习中的潜力。我们没有在学到的世界模型上构建显式规划器，而是将策略优化重新表述为在潜在世界模型中搜索轨迹的扩散过程。基于这一视角，我们从收集到的数据集中提取出一个隐式能量函数来锚定策略，使MBDPO能够在缓解错位的同时，优化策略优化的得分场。我们在多种设置下评估了MBDPO，包括多任务离线预训练、在线学习以及离线到在线的微调。在离线模式下，我们进一步通过在大规模数据集上预训练来研究其扩展行为，观察到随着模型容量的增加，性能持续且单调提升。

## Abstract
Model-based reinforcement learning (RL) can be effectively supported at scale through the use of world models. However, in practice, scaling such approaches remains fundamentally limited. A commonly recognized challenge is model bias and error compounding, which degrade long-horizon predictions. Beyond these issues, we identify a more critical yet underexplored bottleneck: a structural misalignment between search and value learning in existing world model approaches. In particular, policy improvement often relies on value functions induced by a separate, non-search policy, resulting in training inconsistency and ultimately suboptimal learning. To address this limitation, we propose Model-Based Diffusion Policy Optimization (MBDPO) in world models, a framework that unifies search and policy optimization through diffusion policy representations, thereby unlocking the potential of world models for scalable policy learning. Instead of constructing an explicit planner over a learned world model, we reformulate policy optimization as a diffusion process over searched trajectories in latent world models. In this view, we extract an implicit energy function from the collected dataset that anchors the policy, enabling MBDPO to refine the score field for policy optimization while mitigating misalignment. We evaluate MBDPO across a wide range of settings, including multi-task offline pretraining, online learning, and offline-to-online fine-tuning. In the offline regime, we further investigate its scaling behavior by pretraining on large-scale datasets, observing consistent and monotonic performance gains with increasing model capacity.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义

*   **研究背景与动机**：基于模型的强化学习（Model-Based RL）通过让智能体在学到的“世界模型”中想象和规划，被视为实现高样本效率、可扩展决策的关键范式。然而，当前基于世界模型的RL方法在实际性能和扩展性上，仍然难以显著优于无模型（Model-Free）的方法。
*   **核心问题与瓶颈**：本文指出了一个关键但未被充分探索的瓶颈——**搜索与价值学习之间的结构性错位**。具体而言，策略改进（即规划搜索）依赖于对想象轨迹的采样和评估，而价值函数（Q函数）则是基于另一个独立的、非搜索型策略（如回放缓冲区中的行为策略）产生的数据来训练的。这种“训”与“用”的分布差异导致价值函数在搜索访问到的状态-动作对上出现过估计误差，搜索过程则不断利用并放大这些误差，形成一个价值高估和策略偏移的恶性循环，从根本上限制了性能和扩展性。
*   **整体含义**：本文旨在通过统一搜索与策略优化，从原理上解决上述错位问题，从而解锁世界模型在大规模策略学习中的潜力。

### 2. 方法论

*   **核心思想**：提出**基于模型的扩散策略优化（MBDPO）** 框架。它将策略优化重新表述为一个在潜在世界模型中的**扩散过程**，通过扩散策略统一了搜索（想象轨迹）与策略优化（生成最优动作）。
*   **关键技术细节**：
    1.  **隐式能量函数与KL约束**：为了解决错位问题，MBDPO在策略优化目标中引入一个KL散度约束，将搜索策略锚定在数据集的行为策略附近。为了避开行为策略密度难以直接计算的问题，通过对比学习从数据集中学到一个**隐式能量函数**，该函数近似于负的对数行为策略密度，从而将KL约束自然地整合到世界模型的“想象力”轨迹评估中，实现正则化。
    2.  **扩散策略作为优化桥梁**：MBDPO采用去噪扩散概率模型（DDPM）来生成动作序列。其核心是**得分函数**的估计。MBDPO不依赖于前向扩散过程，而是直接在学到的世界模型中进行蒙特卡洛采样，通过潜在动力学模型展开想象轨迹，并根据能量正则化后的累积回报对轨迹进行重加权，以此来估计得分函数。
    3.  **得分匹配与策略优化的等价性**：论文通过定理证明，在满足得分匹配的条件下，扩散策略的逆扩散过程最终生成的精确目标策略，恰好是KL约束优化问题的最优吉布斯分布。这就在数学上建立了世界模型中的得分匹配与真正的策略优化之间的直接等价关系。
*   **算法流程概要**：
    1.  **世界模型与价值迭代**：从回放缓冲区采样轨迹，联合优化编码器、潜在动力学模型、奖励预测器、Q函数和隐式能量函数。
    2.  **扩散策略优化**：利用学到的世界模型展开想象轨迹，通过加权蒙特卡洛估计来近似得分函数，并训练一个得分网络来学习这个得分函数，实现摊销式的快速推理。最终，使用训练好的得分网络通过迭代去噪过程生成优化后的动作序列。

### 3. 实验设计

*   **环境与基准**：在覆盖121个多样化控制任务的4个主流基准上进行评估：
    *   DMControl：用于连续控制任务。
    *   Meta-World：用于多任务和元强化学习的机器人操作任务。
    *   ManiSkill2：接触丰富的通用操作技能任务。
    *   MyoSuite：生理学精确的肌肉骨骼运动控制任务。
    *   还包括视觉RL（基于图像的DMControl）等衍生设定。
*   **对比方法**：与当前最先进的强化学习算法进行了全面对比：
    1.  Soft Actor-Critic（SAC）：代表性无模型基线。
    2.  DreamerV3：领先的基于世界模型的方法。
    3.  TD-MPC 和 TD-MPC2：与本文工作高度相关，是近期最先进的模型预测控制系列方法。
*   **评估设置**：实验覆盖了三种核心训练范式：
    1.  **在线从头训练**：智能体从零开始与环境交互学习。
    2.  **离线多任务预训练**：仅使用固定的、大规模多任务数据集进行训练，并研究其随模型规模扩大的扩展行为。
    3.  **离线到在线微调（O2O）**：先在70个任务上预训练一个通用智能体，再在10个未见过的任务上用极少的环境交互（4万步/任务）进行微调。

### 4. 资源与算力

*   论文明确列出了训练效率分析表，展示了不同设置下的计算资源需求。
*   **GPU型号**：在线训练和评估使用了**NVIDIA A800-SXM4-80GB** GPU。离线多任务训练使用了**NVIDIA-H200** GPU。
*   **训练时长**：
    *   **在线训练**：MBDPO的每环境步训练时间与对比的TD-MPC2相当甚至更快。例如，在Visual RL任务上，MBDPO的平均时间为3.04ms/步，而TD-MPC2为3.55ms/步。
    *   **离线训练**：报告了在单一H200 GPU上训练1000万步所需的小时数。对于340M参数的最大模型，在80个任务的数据集上训练时间约为206.8小时（约8.6天）；在30个任务的数据集上约为202.9小时。

### 5. 实验数量与充分性

*   **实验数量庞大**：论文进行了极其全面的实验评估，包括：
    *   在4个基准、121个任务上的在线训练性能对比。
    *   针对性能瓶颈的两个核心指标（交叉TD误差、动作漂移）的详细分析。
    *   4种不同模型规模（170万到3.4亿参数）下的离线多任务扩展性研究。
    *   离线到在线的微调实验。
    *   针对正则化因子`η`、扩散步数`N`和蒙特卡洛采样数`T`的3项消融研究。
    *   训练与评估时间的全面剖析（针对所有121个任务）。
    *   对80个任务的潜在轨迹和控制性状态进行了可视化分析。
*   **客观性与公平性**：实验设计客观且公平。对比的基线是领域内的主流最强方法。为了隔离策略优化方法的影响，MBDPO严格遵循了TD-MPC系列的训练配置（如归一化、编码器架构），确保了实验对比的焦点明确。

### 6. 论文的主要结论与发现

1.  **性能显著提升**：MBDPO在所有评估基准和训练范式下，都取得了优于现有最强模型基线和无模型基线的性能。
2.  **单调扩展性被解锁**：在离线多任务预训练中，MBDPO是唯一一个随着模型参数量从1.7M增长到340M而展现出**持续、单调的性能增长**的方法。这表明它成功克服了此前世界模型策略学习的扩展瓶颈。
3.  **根本瓶颈被缓解**：通过监测交叉TD误差和动作漂移，实验证实MBDPO有效遏制了价值高估和政策分布偏移的恶性循环。
4.  **跨范式泛化能力强**：MBDPO在在线、离线、以及离线到在线微调等多个范式下均表现优异，展示了其框架的统一性和鲁棒性。
5.  **学到的表征具有物理意义**：对潜在状态轨迹的可视化显示，MBDPO学到的策略在潜在空间中能形成符合物理直觉的结构化行为（例如周期性闭环、目标导向轨迹），而基线方法的轨迹则更杂乱无序。
6.  **计算效率高**：尽管增加了扩散过程，MBDPO的训练和推理开销与强大基线TD-MPC2相比仍处在同一水平，甚至在某些任务上更快。

### 7. 优点

*   **问题洞察深刻**：从理论和实验上精准地识别并形式化了“搜索-价值错位”这个核心瓶颈，这是对现有世界模型RL方法论的有力补充。
*   **方法论创新且优雅**：通过引入扩散策略和隐式能量，在数学上巧妙地将搜索、约束优化和策略生成统一起来，形成了一个原则性强的框架。
*   **实验极其扎实**：在多达121个任务、多种设置、多种模型规模下进行了详尽评估，论证链条完整，证据非常充实。特别是扩展性实验和可视化分析，为理论提供了有力支撑。
*   **理论联系实际**：不仅有理论证明（定理1），还有针对理论指出的瓶颈的实证度量（交叉TD误差、动作漂移），实现了理论与实验的相互印证。

### 8. 不足与局限

*   **推理延迟**：扩散策略需要多步去噪来生成一个动作，这导致其评估（推理）时间显著长于单步前向的基于MPC的方法（如表3所示，有时是后者的近两倍），可能对实时性要求极高的应用场景构成挑战。
*   **环境与规模限制**：
    *   所有评估仍在**模拟环境**中进行，未在真实世界机器人上进行验证，其实用性和鲁棒性有待检验。
    *   受限于计算资源，尚未将模型扩展到十亿（1B）参数以上的“基础模型”规模进行测试。
*   **对表示的依赖性**：论文在分析和可视化中明确指出了学到的潜在表征质量的重要性（如“Hopper Hop”任务的失败归因于表征偏差），这表明方法的成功高度依赖一个良好预训练或学到的世界模型，其鲁棒性的边界尚不完全清晰。

（完）
